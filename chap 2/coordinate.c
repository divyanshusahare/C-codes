#include<stdio.h>
int main()
{
	float x,y;
	printf("Enter the coordinate of a(x,y)");
	scanf("%f %f",&x,&y);
	if(x==0&&y==1)
	{
		if(y==0)
		printf("The point is lise on origin")
		else
	}
	else
	{
		if(Y==0)
		print``()
	}
	
}
